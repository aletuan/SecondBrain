# Entities

Create one note per **named** thing (person, product, repo, org) when it appears across multiple captures.

Suggested frontmatter:

```yaml
---
type: entity
kind: person   # or product | repo | org | concept
aliases: []
---
```

Link **from** topic pages and **to** relevant `Captures/…` notes.
